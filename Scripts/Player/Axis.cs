/// <summary>
/// Represents the axis that the player can use to move.
/// </summary>
public static class Axis
{
    /// <summary>
    /// The axis that makes the player move horizontally.
    /// </summary>
    public const string Horizontal = nameof(Horizontal);
}